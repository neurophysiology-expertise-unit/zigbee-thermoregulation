-r requirements.txt
# PySide6 6.11.1 and 6.10.3 both fail to import on Windows (DLL load error,
# likely a packaging issue) -- 6.8.0.2 works. Pin an upper bound so a fresh
# install doesn't silently grab a broken newer build; bump it deliberately
# after checking `python -c "from PySide6 import QtCore"`.
PySide6>=6.8,<6.10
matplotlib>=3.7
numpy>=1.24
